# Known Issues

Tracked defects and parked fixes. Each entry records what's wrong, where it lives,
severity, and status. Work on a parked item happens on its own branch.

_No open issues currently tracked. (AW-1 — "gateway with zero active windows
silently falls through to JIT" — was fixed: the poller now always keeps the
`approved_windows.sh` handler deployed and window-less hosts hard-reject
presented tokens. See CHANGELOG.)_
